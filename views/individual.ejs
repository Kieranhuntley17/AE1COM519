<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%=employees[0].name%></title>
    <link rel="stylesheet" href="../css/styles.css" type="text/css">
</head>
<body>
    <div class="container">
        <a href="/employees"><p>back</p></a>
        <ul>
            <li>Name: <%=employees[0].name%>
                <form method="POST" action="/employees/editname/<%= employees[0]._id %>">
                    <input
                        type="text"
                        class="form-control"
                        id="editname"
                        name="editname"
                        placeholder="Enter New Name"
                    />
                    <button type="submit">Update Name</button>
                </form>
            </li>

            <li>Position: <%=employees[0].position%> 
                <form method="POST" action="/employees/editposition/<%= employees[0]._id %>">
                    <input
                        type="text"
                        class="form-control"
                        id="editposition"
                        name="editposition"
                        placeholder="Enter New Position"
                    />
                    <button type="submit">Update Position</button>
                </form>
            </li>
            <li>Start Date: <%=employees[0].dateofstart%></li><br>

            <li>Mobile Number: <%=employees[0].mobile%>
                <form method="POST" action="/employees/editmobile/<%= employees[0]._id %>">
                    <input
                        type="text"
                        class="form-control"
                        id="editmobile"
                        name="editmobile"
                        placeholder="Enter New Number"
                    />
                    <button type="submit">Update Mobile</button>
                </form>
            </li>
            
            <li>Agreed Hours: <%=employees[0].agreedhours%> hour(s)
                <form method="POST" action="/employees/edithours/<%= employees[0]._id %>">
                    <input
                        type="text"
                        class="form-control"
                        id="edithours"
                        name="edithours"
                        placeholder="Enter New Hours"
                    />
                    <button type="submit">Update Hours</button>
                </form>
            </li>
            <li>Email: <%=employees[0].email%> 
                <form method="POST" action="/employees/editemail/<%= employees[0]._id %>">
                    <input
                        type="text"
                        class="form-control"
                        id="editemail"
                        name="editemail"
                        placeholder="Enter New Email"
                    />
                    <button type="submit">Update Email</button>
                </form>
            </li>
        </ul>
        <a href="/employees/delete/<%= employees[0]._id %>"><h3 id="delete">Delete User</h3></a>
    </div>
    
</body>
</html>